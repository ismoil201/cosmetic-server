spring:
  datasource:
    url: jdbc:mysql://gondola.proxy.rlwy.net:48131/railway?useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true&serverTimezone=UTC
    username: root
    password: kofWgIwqhZqDqbcPPlYktBcgFQfbKlci
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: ${PORT:8080}
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html   # foydalanuvchi shu bilan kiradi, u /swagger-ui/index.html ga redirect bo‘ladi
    display-request-duration: true
    operationsSorter: method
    tagsSorter: alpha
    docExpansion: none
    disable-swagger-default-url: true