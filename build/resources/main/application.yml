spring:
  datasource:
    url: jdbc:mysql://gondola.proxy.rlwy.net:48131/railway?useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true&serverTimezone=UTC
    username: root
    password: kofWgIwqhZqDqbcPPlYktBcgFQfbKlci
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: ${PORT:8080}
